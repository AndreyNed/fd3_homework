'use strict';

// todo import actions` types

const initState = {

};

function rdMain ( state = initState, action ) {
    switch ( action.type ) {
        default:
            return state;
    }
}

export default rdMain;